[project]
name = "features"
version = "0.1.0"
description = "Central repository for Vindicta Platform feature testing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "behave>=1.2.6",
]

[tool.uv]
package = true
